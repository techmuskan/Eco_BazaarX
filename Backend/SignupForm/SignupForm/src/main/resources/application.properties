spring.application.name=SignupForm

server.port=${SERVER_PORT:8080}

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/LoginDetails}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:10Muskan@2005}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=${JPA_DDL_AUTO:update}
spring.jpa.open-in-view=false

spring.mail.host=${MAIL_HOST:smtp.gmail.com}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:anshikasingh010105@gmail.com}
spring.mail.password=${MAIL_PASSWORD:nxvggjlaefneksgr}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.cors.allowed-origins=${APP_CORS_ALLOWED_ORIGINS:http://localhost:*,http://127.0.0.1:*,http://[::1]:*,https://localhost:*,https://127.0.0.1:*}
app.public-base-url=${APP_PUBLIC_BASE_URL:http://localhost:8080}

app.bootstrap.admin.enabled=${BOOTSTRAP_ADMIN_ENABLED:false}
app.bootstrap.admin.name=${BOOTSTRAP_ADMIN_NAME:EcoBazaar Admin}
app.bootstrap.admin.email=${BOOTSTRAP_ADMIN_EMAIL:admin@ecobazar.local}
app.bootstrap.admin.password=${BOOTSTRAP_ADMIN_PASSWORD:admin@123}
app.bootstrap.admin.phone=${BOOTSTRAP_ADMIN_PHONE:9644870597}
app.signup.admin-enabled=${APP_SIGNUP_ADMIN_ENABLED:false}

app.jwt.secret=${APP_JWT_SECRET:replace-me-with-a-long-random-jwt-secret-12345}
app.jwt.expiration-ms=${APP_JWT_EXPIRATION_MS:7200000}
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
